<!DOCTYPE html>
<html lang="bn">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= site.name %> | মেডিকেল ও ডেন্টাল ভর্তি কোচিং</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Tiro+Bangla&family=Hind+Siliguri:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>

  <header class="site-header">
    <div class="wrap header-inner">
      <a href="/" class="brand">
        <span class="brand-mark">M</span>
        <span class="brand-name"><%= site.name %></span>
      </a>

      <input type="checkbox" id="nav-toggle" class="nav-toggle">
      <label for="nav-toggle" class="nav-burger" aria-label="মেনু খুলুন">
        <span></span><span></span><span></span>
      </label>

      <nav class="site-nav">
        <a href="/" class="<%= active === 'home' ? 'active' : '' %>">হোম</a>
        <a href="/courses" class="<%= active === 'courses' ? 'active' : '' %>">ব্যাচ ও ফি</a>
        <a href="/success" class="<%= active === 'success' ? 'active' : '' %>">মেধা তালিকা</a>
        <a href="/contact" class="<%= active === 'contact' ? 'active' : '' %>">যোগাযোগ</a>
        <a href="/courses#admission" class="nav-cta">ভর্তি হও</a>
      </nav>
    </div>
  </header>

  <main>
